var splide = new Splide( '.splide', {
    width: '40%',
    height: '700px',
    // perPage: 1,
    // perMove: 1,
    // autoWidth: true,
    // focus: 'center',
    breakpoints: {
        645: {
            width: '100%',
        }
    }
} );
splide.mount();